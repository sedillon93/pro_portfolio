'use strict';

// page('/home', portfolio.homeController.showHome);
// page('/projects', portfolio.projectController.showProjects);
// page('/jobs', portfolio.jobController.showJobs);
// page('/contact', portfolio.contactController.showContacts);

page();
